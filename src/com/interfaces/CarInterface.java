package com.interfaces;

public interface CarInterface { //inside interfaces it is possible to define abstract methods     all interface abstract methods are public
    void start(); //interface abstract methods cannot have a body {}
    void move(int speed);
}
